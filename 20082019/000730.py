def funcion3(x,y): #se define funcion de dos variables
    return x+y #lo que arroja la funcion al ser llamada
e = funcion3(1,2) #se define variable con la funcion para poder posteriormente inprimirla. Esto es necesario ya que al tener return en la funcion, no lo imprime de por si.

print e # para imprimir la funcion
